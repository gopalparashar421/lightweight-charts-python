# Attach Primitive

Demonstrates attaching a custom primitive using a raw JavaScript object that
implements the `ISeriesPrimitive` interface, extending the chart with bespoke rendering.

**Screenshot**

> _Screenshot pending. Run the example and add `attach_primitive.png` here._

## Run

```bash
python examples/attach_primitive/attach_primitive.py
```
